# Next-AI 环境变量配置示例
# 使用说明:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际情况修改配置
# 3. 确保 .env 文件不会被提交到版本控制系统

# ========== 镜像版本 ==========
# Next-AI 镜像版本标签
NEXT_AI_VERSION=latest

# ========== 应用配置 ==========
# Gin 模式: debug(开发), release(生产)
GIN_MODE=release

# 禁止新用户注册（生产环境建议设为 true）
DISABLE_REGISTRATION=false

# 应用服务端口
APP_PORT=8080

# 前端服务端口
FRONTEND_PORT=80

# 文件大小限制（MB）
MAX_FILE_SIZE_MB=50

# ========== 数据库配置 ==========
# 数据库类型
DB_DRIVER=postgres

# 数据库用户名
DB_USER=nextai

# 数据库密码
DB_PASSWORD=nextai123

# 数据库名称
DB_NAME=nextai

# Elasticsearch 配置（向量检索）
ELASTICSEARCH_ADDR=elasticsearch:9200
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_PORT=9200

# ========== Redis 配置 ==========
# Redis 密码（留空表示无密码）
REDIS_PASSWORD=

# Redis 数据库索引
REDIS_DB=0

# ========== AI 模型配置 ==========
# Ollama 本地模型服务
OLLAMA_BASE_URL=http://host.docker.internal:11434

# OpenAI 兼容接口
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini

# 阿里云 DashScope (通义千问)
DASHSCOPE_ACCESS_KEY=

# 初始化模型配置（首次启动时使用）
INIT_LLM_MODEL_NAME=
INIT_LLM_MODEL_BASE_URL=
INIT_LLM_MODEL_API_KEY=
INIT_EMBEDDING_MODEL_NAME=
INIT_EMBEDDING_MODEL_BASE_URL=
INIT_EMBEDDING_MODEL_API_KEY=
INIT_EMBEDDING_MODEL_DIMENSION=

# ========== 认证配置 ==========
# JWT 密钥（请修改为随机字符串）
JWT_SECRET=next-ai-jwt-secret-change-me

# ========== 存储配置 ==========
# 文件存储类型: local, minio
STORAGE_TYPE=local

# 本地存储路径
LOCAL_STORAGE_BASE_DIR=/data/files

# ========== 可选服务 ==========
# MinIO 对象存储（当 STORAGE_TYPE=minio 时需要）
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ACCESS_KEY_ID=minioadmin
MINIO_SECRET_ACCESS_KEY=minioadmin
MINIO_BUCKET_NAME=
